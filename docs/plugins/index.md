# Extending BlueSky with plugins

BlueSky plugins let you add new stack commands, run periodic logic, or
replace built-in simulation behaviour entirely — all without modifying
BlueSky's own source. This section covers activating existing plugins and
writing your own, from a first simple example to more advanced patterns
like replacing core algorithms or drawing on the radar view.
